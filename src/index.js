// By the way: Chuck Norris doesn't need try-catch, exceptions are too afraid to raise.

import React from 'react';
import { render } from 'react-dom';
import App from './App';

import './App.css';

render(<App />, document.querySelector('#root'));
